--[[
    ╔═══════════════════════════════════════════════════════════════╗
    ║  SHERLOCK APEX V94 ULTRA - OPTIMIZED EDITION                 ║
    ║  Versão Melhorada com Fixes de Memory Leak                   ║
    ║  e Arquitetura Modular                                       ║
    ╚═══════════════════════════════════════════════════════════════╝
    
    MELHORIAS IMPLEMENTADAS:
    ✓ Memory leak fixes no ITEM ESP e BRING ITEMS
    ✓ Cleanup automático de objetos destruídos
    ✓ Fallbacks para funções de exploit indisponíveis
    ✓ Arquitetura modular com classe ApexScript
    ✓ Sistema de eventos melhorado
    ✓ Performance otimizada com cache
    ✓ Melhor detecção de team
    ✓ UI mais responsiva com tween suave
    ✓ Botão fechar removido (EXIT já existe)
    ✓ Kill Aura removida (sistema de saúde personalizado)
    ✓ FLY renomeado para VOAR com Speed Hack integrado
    ✓ BRING volta ao original (funcional)
    ✓ Multiplicador de Wooden Floor (50x) - DETECTOR INTELIGENTE
    ✓ Otimizações de performance para evitar lag
]]

-- ====== SERVIÇOS E VARIÁVEIS GLOBAIS ======
local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- ====== CLASSE PRINCIPAL DO SCRIPT ======
local ApexScript = {}
ApexScript.__index = ApexScript

function ApexScript.new()
    local self = setmetatable({}, ApexScript)
    
    -- Configurações
    self.Config = {
        bringItemsSpeed = 1.5,
        espUpdateSpeed = 2,
        playerEspUpdateSpeed = 0.1,
    }
    
    -- Estados
    self.States = {
        bringMetals = false,
        bringItems = false,
        speedHack = false,
        speedHackWhileFlying = false,
        fly = false,
        infJump = false,
        itemEsp = false,
        playerEsp = false,
        multiplyFloors = false,
    }
    
    -- Rastreamento de objetos (OTIMIZADO COM WEAK REFERENCES)
    self.Tracking = {
        broughtItems = setmetatable({}, {__mode = "k"}), -- Weak key references
        espLabels = setmetatable({}, {__mode = "k"}),
        playerHighlights = setmetatable({}, {__mode = "k"}),
        playerLabels = setmetatable({}, {__mode = "k"}),
        activeThreads = {},
        multipliedFloors = {},
        itemCount = 0, -- Contador de itens rastreados
    }
    
    -- Dados salvos
    self.OriginalGravity = workspace.Gravity
    self.FlySpeedMultiplier = 5
    
    -- Configurações de itens
    self.MetaisParaTrazer = {"Metal", "ScrapMetal", "ESP Metal"}
    self.ItensParaTrazer = {"BrokenCrate", "BrokenCrate2", "Plank"}
    self.ArmasSuportadas = {"Machete", "Machado", "Arpão", "Machete ", "Machado ", "Arpão "}
    self.LootKeywords = {"wood", "plastic", "scrap", "metal", "barrel", "crate", "box", "plank", "bolt", "leaf", "chest"}
    
    -- UI
    self.UI = {}
    self.MinimizedState = false
    
    -- Contador para cleanup periódico
    self.CleanupCounter = 0
    self.CleanupInterval = 100 -- Cleanup a cada 100 iterações
    
    -- Cache para rastreamento de Wooden Floors já multiplicados
    self.WoodenFloorCache = setmetatable({}, {__mode = "k"})
    
    -- Armazenar o ClassName esperado para Wooden Floor
    self.WoodenFloorClassName = nil
    
    return self
end

-- ====== UTILITÁRIOS ======
function ApexScript:SafeCall(func, ...)
    local success, result = pcall(func, ...)
    if not success then
        warn("[Sherlock] Erro em SafeCall: " .. tostring(result))
    end
    return result
end

function ApexScript:IsValidCharacter(character)
    return character and character.Parent and character:FindFirstChild("HumanoidRootPart") and character:FindFirstChildOfClass("Humanoid")
end

function ApexScript:IsTeammate(player)
    if not player then return false end
    if not LP.Team then return false end
    return player.Team == LP.Team
end

function ApexScript:GetPlayerFromCharacter(character)
    return Players:FindFirstChildOfClass("Player", function(p)
        return p.Character == character
    end) or Players:GetPlayerFromCharacter(character)
end

-- Função para detectar Wooden Floor por análise estrutural
function ApexScript:IsWoodenFloor(item)
    -- Se ainda não descobrimos o ClassName, tentar detectar
    if not self.WoodenFloorClassName then
        -- Verificar propriedades conhecidas de Wooden Floor
        -- Typically Wooden Floors têm estrutura específica no backpack
        
        -- Se o nome é apenas números:números (formato 1780755586:9814767070)
        local itemName = item.Name
        local hasColonFormat = string.find(itemName, "^%d+:%d+$")
        
        if hasColonFormat then
            -- Verificar se tem propriedades de um item de construção
            if item:IsA("Tool") or item:IsA("Model") or item:IsA("BasePart") then
                -- Verificar se a descrição ou class name dá alguma pista
                if item:FindFirstChild("Handle") or item:FindFirstChild("Mesh") then
                    print("[Sherlock] DETECTADO: Wooden Floor com nome: " .. itemName)
                    self.WoodenFloorClassName = itemName:match("^%d+"):gsub("%d", "X")
                    return true
                end
            end
        end
        return false
    end
    
    -- Se já detectou, verificar padrão
    local itemName = item.Name
    return string.find(itemName, "^%d+:%d+$") ~= nil
end

-- Cleanup periódico para liberar memória
function ApexScript:PeriodicCleanup()
    self.CleanupCounter = self.CleanupCounter + 1
    
    if self.CleanupCounter >= self.CleanupInterval then
        self.CleanupCounter = 0
        
        -- Forçar coleta de lixo
        collectgarbage("collect")
        
        -- Resetar caches se necessário
        if self.Tracking.itemCount > 5000 then
            print("[Sherlock] Limpeza profunda executada. Itens rastreados: " .. self.Tracking.itemCount)
            self.Tracking.itemCount = 0
        end
    end
end

-- ====== FUNÇÕES DE EXPLOIT (COM FALLBACKS) ======
function ApexScript:GetExploitFunctions()
    local virtUser = nil
    local getGarbageCollector = nil
    local fireTouchInterest = nil
    
    -- Tenta carregar VirtualUser
    if typeof(VirtualUser) == "userdata" then
        virtUser = VirtualUser
    elseif game:FindFirstChildOfClass("VirtualUser") then
        virtUser = game:FindFirstChildOfClass("VirtualUser")
    else
        pcall(function()
            virtUser = game:GetService("VirtualUser")
        end)
    end
    
    -- Tenta carregar getgc
    if type(getgc) == "function" then
        getGarbageCollector = getgc
    end
    
    -- Tenta carregar firetouchinterest
    if type(firetouchinterest) == "function" then
        fireTouchInterest = firetouchinterest
    end
    
    return {
        VirtualUser = virtUser,
        getgc = getGarbageCollector,
        fireTouchInterest = fireTouchInterest,
    }
end

function ApexScript:ClickTarget(part)
    local screenPos, onScreen = Camera:WorldToScreenPoint(part.Position)
    if not onScreen then return end
    
    local exploit = self:GetExploitFunctions()
    if exploit.VirtualUser then
        exploit.VirtualUser:CaptureController()
        exploit.VirtualUser:Button1Down(Vector2.new(screenPos.X, screenPos.Y), Camera.CFrame)
        exploit.VirtualUser:Button1Up(Vector2.new(screenPos.X, screenPos.Y), Camera.CFrame)
    end
end

-- ====== UI CONSTRUCTION ======
function ApexScript:BuildUI()
    local ApexHub = Instance.new("ScreenGui")
    ApexHub.Name = "Apex_V94_Improved"
    ApexHub.Parent = game:GetService("CoreGui")
    
    local Main = Instance.new("Frame")
    Main.Name = "MainFrame"
    Main.Parent = ApexHub
    Main.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
    Main.BorderSizePixel = 0
    Main.Position = UDim2.new(0.5, -130, 0.5, -200)
    Main.Size = UDim2.new(0, 260, 0, 440)
    Main.Active = true
    Main.Draggable = true
    
    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 15)
    UICorner.Parent = Main
    
    local UIStroke = Instance.new("UIStroke")
    UIStroke.Thickness = 3
    UIStroke.Color = Color3.fromRGB(0, 255, 120)
    UIStroke.Parent = Main
    
    -- Título
    local Title = Instance.new("TextLabel")
    Title.Parent = Main
    Title.Size = UDim2.new(0.85, 0, 0, 50)
    Title.Position = UDim2.new(0, 10, 0, 5)
    Title.Text = "SHERLOCK APEX V94"
    Title.TextColor3 = Color3.new(1, 1, 1)
    Title.Font = Enum.Font.GothamBlack
    Title.TextSize = 18
    Title.BackgroundTransparency = 1
    
    -- Botão Minimize
    local MinimizeBtn = Instance.new("TextButton")
    MinimizeBtn.Parent = Main
    MinimizeBtn.Size = UDim2.new(0, 40, 0, 40)
    MinimizeBtn.Position = UDim2.new(1, -45, 0, 5)
    MinimizeBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 120)
    MinimizeBtn.Text = "−"
    MinimizeBtn.TextColor3 = Color3.fromRGB(12, 12, 18)
    MinimizeBtn.Font = Enum.Font.GothamBold
    MinimizeBtn.TextSize = 24
    MinimizeBtn.BorderSizePixel = 0
    
    local minCorner = Instance.new("UICorner")
    minCorner.CornerRadius = UDim.new(0, 8)
    minCorner.Parent = MinimizeBtn
    
    -- Conteúdo
    local Content = Instance.new("ScrollingFrame")
    Content.Parent = Main
    Content.Position = UDim2.new(0, 15, 0, 60)
    Content.Size = UDim2.new(1, -30, 1, -75)
    Content.BackgroundTransparency = 1
    Content.ScrollBarThickness = 0
    
    local UIList = Instance.new("UIListLayout")
    UIList.Parent = Content
    UIList.Padding = UDim.new(0, 8)
    
    -- Store UI
    self.UI.Hub = ApexHub
    self.UI.Main = Main
    self.UI.Content = Content
    self.UI.Title = Title
    self.UI.MinimizeBtn = MinimizeBtn
    self.UI.OriginalSize = Main.Size
    
    -- Minimize toggle
    MinimizeBtn.MouseButton1Click:Connect(function()
        self:ToggleMinimize()
    end)
    
    return Content
end

function ApexScript:ToggleMinimize()
    self.MinimizedState = not self.MinimizedState
    if self.MinimizedState then
        self.UI.Main:TweenSize(UDim2.new(0, 260, 0, 50), Enum.EasingDirection.InOut, Enum.EasingStyle.Quad, 0.3, true)
        self.UI.Content.Visible = false
        self.UI.MinimizeBtn.Text = "+"
    else
        self.UI.Main:TweenSize(self.UI.OriginalSize, Enum.EasingDirection.InOut, Enum.EasingStyle.Quad, 0.3, true)
        self.UI.Content.Visible = true
        self.UI.MinimizeBtn.Text = "−"
    end
end

function ApexScript:MakeButton(text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 45)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10
    btn.Parent = self.UI.Content
    btn.BorderSizePixel = 0
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        callback(btn)
    end)
    
    return btn
end

-- ====== FEATURES ======

-- 1. BRING METALS (ORIGINAL - FUNCIONAL)
function ApexScript:ToggleBringMetals(btn)
    self.States.bringMetals = not self.States.bringMetals
    btn.Text = self.States.bringMetals and "BRING METALS: ACTIVE" or "BRING METALS: OFF"
    
    if not self.States.bringMetals then
        self.Tracking.broughtItems = setmetatable({}, {__mode = "k"})
        self.Tracking.itemCount = 0
    end
    
    if self.States.bringMetals then
        local thread = task.spawn(function()
            while self.States.bringMetals do
                self:SafeCall(function()
                    if self:IsValidCharacter(LP.Character) then
                        local meuCFrame = LP.Character.HumanoidRootPart.CFrame * CFrame.new(0, 3, -8)
                        
                        for _, v in pairs(workspace:GetDescendants()) do
                            if (v:IsA("BasePart") or v:IsA("Model")) and not self.Tracking.broughtItems[v] then
                                local metalValido = false
                                for _, nomeItem in pairs(self.MetaisParaTrazer) do
                                    if v.Name == nomeItem then metalValido = true; break end
                                end
                                
                                if metalValido and not v:IsDescendantOf(LP.Character) then
                                    self.Tracking.broughtItems[v] = true
                                    self.Tracking.itemCount = self.Tracking.itemCount + 1
                                    self:MoveObject(v, meuCFrame)
                                end
                            end
                        end
                        
                        -- Cleanup periódico
                        self:PeriodicCleanup()
                    end
                end)
                task.wait(self.Config.bringItemsSpeed)
            end
        end)
        table.insert(self.Tracking.activeThreads, thread)
    end
end

-- 2. BRING ITEMS (ORIGINAL - FUNCIONAL)
function ApexScript:ToggleBringItems(btn)
    self.States.bringItems = not self.States.bringItems
    btn.Text = self.States.bringItems and "BRING ITEMS: ACTIVE" or "BRING ITEMS: OFF"
    
    if not self.States.bringItems then
        self.Tracking.broughtItems = setmetatable({}, {__mode = "k"})
        self.Tracking.itemCount = 0
    end
    
    if self.States.bringItems then
        local thread = task.spawn(function()
            while self.States.bringItems do
                self:SafeCall(function()
                    if self:IsValidCharacter(LP.Character) then
                        local meuCFrame = LP.Character.HumanoidRootPart.CFrame * CFrame.new(0, 3, -8)
                        
                        for _, v in pairs(workspace:GetDescendants()) do
                            if (v:IsA("BasePart") or v:IsA("Model")) and not self.Tracking.broughtItems[v] then
                                local itemValido = false
                                for _, nomeItem in pairs(self.ItensParaTrazer) do
                                    if v.Name == nomeItem then
                                        itemValido = true
                                        break
                                    end
                                end
                                
                                if itemValido and not v:IsDescendantOf(LP.Character) then
                                    self.Tracking.broughtItems[v] = true
                                    self.Tracking.itemCount = self.Tracking.itemCount + 1
                                    
                                    if v:IsA("Model") then
                                        for _, part in pairs(v:GetDescendants()) do
                                            if part:IsA("BasePart") then
                                                part.Anchored = false
                                                part.CanCollide = true
                                                part.AssemblyLinearVelocity = Vector3.new(0,0,0)
                                            end
                                        end
                                        if v.PrimaryPart then
                                            v:SetPrimaryPartCFrame(meuCFrame)
                                        else
                                            v:PivotTo(meuCFrame)
                                        end
                                    elseif v:IsA("BasePart") then
                                        v.Anchored = false
                                        v.CanCollide = true
                                        v.AssemblyLinearVelocity = Vector3.new(0,0,0)
                                        v.CFrame = meuCFrame
                                    end
                                end
                            end
                        end
                        
                        -- Cleanup periódico
                        self:PeriodicCleanup()
                    end
                end)
                task.wait(self.Config.bringItemsSpeed)
            end
        end)
        table.insert(self.Tracking.activeThreads, thread)
    end
end

-- Função auxiliar para mover objetos
function ApexScript:MoveObject(obj, targetCFrame)
    if obj:IsA("Model") then
        for _, part in pairs(obj:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Anchored = false
                part.CanCollide = true
                part.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            end
        end
        if obj.PrimaryPart then
            obj:SetPrimaryPartCFrame(targetCFrame)
        else
            obj:PivotTo(targetCFrame)
        end
    elseif obj:IsA("BasePart") then
        obj.Anchored = false
        obj.CanCollide = true
        obj.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        obj.CFrame = targetCFrame
    end
end

-- 3. SPEED HACK
function ApexScript:ToggleSpeedHack(btn)
    self.States.speedHack = not self.States.speedHack
    btn.Text = self.States.speedHack and "SPEED: 5X ACTIVE" or "SPEED HACK: OFF"
    
    local thread = task.spawn(function()
        while self.States.speedHack do
            self:SafeCall(function()
                if self:IsValidCharacter(LP.Character) then
                    LP.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = 995
                end
            end)
            task.wait(0.1)
        end
        if self:IsValidCharacter(LP.Character) then
            LP.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = 16
        end
    end)
    table.insert(self.Tracking.activeThreads, thread)
end

-- 4. FLY COM SPEED HACK INTEGRADO
function ApexScript:ToggleFly(btn)
    self.States.fly = not self.States.fly
    btn.Text = self.States.fly and "VOAR: ACTIVE" or "VOAR: OFF"
    
    if self.States.fly then
        local thread = task.spawn(function()
            if self:IsValidCharacter(LP.Character) then
                local hum = LP.Character:FindFirstChildOfClass("Humanoid")
                local root = LP.Character.HumanoidRootPart
                
                for i = 1, 25 do
                    if not self.States.fly then break end
                    hum:ChangeState(Enum.HumanoidStateType.Jumping)
                    root.AssemblyLinearVelocity = Vector3.new(root.AssemblyLinearVelocity.X, 45, root.AssemblyLinearVelocity.Z)
                    task.wait(0.02)
                end
                
                while self.States.fly do
                    self:SafeCall(function()
                        workspace.Gravity = 0
                        -- Speed hack integrado enquanto voa
                        hum.WalkSpeed = 995
                        
                        if root and hum then
                            local moveDirection = hum.MoveDirection
                            if moveDirection.Magnitude > 0 then
                                local flyVelocity = moveDirection * (hum.WalkSpeed * self.FlySpeedMultiplier)
                                root.AssemblyLinearVelocity = Vector3.new(flyVelocity.X, 0, flyVelocity.Z)
                            else
                                root.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            end
                        end
                    end)
                    task.wait(0.01)
                end
            end
        end)
        table.insert(self.Tracking.activeThreads, thread)
    else
        workspace.Gravity = self.OriginalGravity
        if self:IsValidCharacter(LP.Character) then
            LP.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = 16
        end
    end
end

-- 5. INFINITE JUMP
function ApexScript:ToggleInfiniteJump(btn)
    self.States.infJump = not self.States.infJump
    btn.Text = self.States.infJump and "INF JUMP: ON" or "INF JUMP: OFF"
end

function ApexScript:InitializeInfiniteJump()
    UIS.JumpRequest:Connect(function()
        if self.States.infJump and self:IsValidCharacter(LP.Character) then
            LP.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end

-- 6. MULTIPLICADOR DE WOODEN FLOOR (50x) - DETECTOR INTELIGENTE
function ApexScript:ToggleMultiplyFloors(btn)
    self.States.multiplyFloors = not self.States.multiplyFloors
    btn.Text = self.States.multiplyFloors and "MULTIPLY FLOORS: DETECTING..." or "MULTIPLY FLOORS: OFF"
    
    if not self.States.multiplyFloors then
        -- Limpar floors multiplicados se desativar
        for _, floor in pairs(self.Tracking.multipliedFloors) do
            if floor and floor.Parent then
                floor:Destroy()
            end
        end
        self.Tracking.multipliedFloors = {}
        self.WoodenFloorCache = setmetatable({}, {__mode = "k"})
        btn.Text = "MULTIPLY FLOORS: OFF"
    else
        local thread = task.spawn(function()
            local foundFloor = false
            
            while self.States.multiplyFloors do
                self:SafeCall(function()
                    if self:IsValidCharacter(LP.Character) then
                        local backpack = LP:FindFirstChild("Backpack")
                        if backpack then
                            for _, item in pairs(backpack:GetChildren()) do
                                -- Detectar Wooden Floor por padrão de nome (números:números)
                                if self:IsWoodenFloor(item) and not self.WoodenFloorCache[item] then
                                    if not foundFloor then
                                        print("[Sherlock] ✓ DETECTADO WOODEN FLOOR: " .. item.Name)
                                        foundFloor = true
                                        btn.Text = "MULTIPLY FLOORS: ACTIVE"
                                    end
                                    
                                    print("[Sherlock] Multiplicando: " .. item.Name .. " (50x)")
                                    
                                    -- Duplicar o wooden floor 50 vezes
                                    for i = 1, 50 do
                                        local newFloor = item:Clone()
                                        newFloor.Parent = backpack
                                        table.insert(self.Tracking.multipliedFloors, newFloor)
                                    end
                                    
                                    self.WoodenFloorCache[item] = true
                                    print("[Sherlock] ✓ 50 cópias criadas!")
                                end
                            end
                        end
                    end
                end)
                task.wait(0.5)
            end
        end)
        table.insert(self.Tracking.activeThreads, thread)
    end
end

-- 7. INSTANT INTERACT
function ApexScript:InstantInteract()
    local exploit = self:GetExploitFunctions()
    if exploit.getgc then
        for _, v in pairs(exploit.getgc(true)) do
            if type(v) == "table" and (rawget(v, "CollectionTime") or rawget(v, "HoldTime")) then
                v.CollectionTime = 0
                v.HoldTime = 0
            end
        end
        return true
    end
    return false
end

-- 8. ITEM ESP
function ApexScript:ToggleItemEsp(btn)
    self.States.itemEsp = not self.States.itemEsp
    btn.Text = self.States.itemEsp and "ITEM ESP: ACTIVE" or "ITEM ESP: OFF"
    
    if not self.States.itemEsp then
        for _, label in pairs(self.Tracking.espLabels) do
            if label and label.Parent then
                label:Destroy()
            end
        end
        self.Tracking.espLabels = setmetatable({}, {__mode = "k"})
    else
        local thread = task.spawn(function()
            while self.States.itemEsp do
                self:SafeCall(function()
                    for _, v in pairs(workspace:GetDescendants()) do
                        if (v:IsA("BasePart") or v:IsA("Model")) and v.Parent then
                            local nameLower = v.Name:lower()
                            local isLoot = false
                            
                            for _, key in pairs(self.LootKeywords) do
                                if nameLower:find(key) then
                                    isLoot = true
                                    break
                                end
                            end
                            
                            if isLoot and v.Name ~= "Hitbox" and v.Name ~= "WoodenFloor" and not self.Tracking.espLabels[v] then
                                local bg = Instance.new("BillboardGui", v)
                                bg.Name = "SherlockLabel"
                                bg.Size = UDim2.new(0, 120, 0, 50)
                                bg.AlwaysOnTop = true
                                
                                local tl = Instance.new("TextLabel", bg)
                                tl.Size = UDim2.new(1, 0, 1, 0)
                                tl.BackgroundTransparency = 1
                                tl.TextColor3 = Color3.new(0, 1, 1)
                                tl.Text = "[ " .. v.Name .. " ]"
                                tl.Font = Enum.Font.GothamBold
                                tl.TextSize = 11
                                tl.TextStrokeTransparency = 0.5
                                
                                self.Tracking.espLabels[v] = bg
                            end
                        end
                    end
                end)
                task.wait(self.Config.espUpdateSpeed)
            end
        end)
        table.insert(self.Tracking.activeThreads, thread)
    end
end

-- 9. PLAYER ESP
function ApexScript:TogglePlayerEsp(btn)
    self.States.playerEsp = not self.States.playerEsp
    btn.Text = self.States.playerEsp and "PLAYER ESP: ACTIVE" or "PLAYER ESP: OFF"
    
    if not self.States.playerEsp then
        for _, p in pairs(Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("PlayerHighlight") then
                p.Character.PlayerHighlight:Destroy()
            end
            if self.Tracking.playerLabels[p] and self.Tracking.playerLabels[p].Parent then
                self.Tracking.playerLabels[p]:Destroy()
            end
        end
        self.Tracking.playerHighlights = setmetatable({}, {__mode = "k"})
        self.Tracking.playerLabels = setmetatable({}, {__mode = "k"})
    else
        local thread = task.spawn(function()
            while self.States.playerEsp do
                self:SafeCall(function()
                    for _, p in pairs(Players:GetPlayers()) do
                        if p ~= LP and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                            local char = p.Character
                            
                            -- Highlight
                            local hl = char:FindFirstChild("PlayerHighlight") or Instance.new("Highlight", char)
                            hl.Name = "PlayerHighlight"
                            hl.FillColor = Color3.new(0.5, 0, 1)
                            hl.OutlineColor = Color3.new(1, 1, 1)
                            
                            -- Label
                            if not char.HumanoidRootPart:FindFirstChild("PlayerLabel") then
                                local b = Instance.new("BillboardGui", char.HumanoidRootPart)
                                b.Name = "PlayerLabel"
                                b.Size = UDim2.new(0, 150, 0, 50)
                                b.AlwaysOnTop = true
                                b.ExtentsOffset = Vector3.new(0, 3, 0)
                                
                                local t = Instance.new("TextLabel", b)
                                t.Size = UDim2.new(1, 0, 1, 0)
                                t.BackgroundTransparency = 1
                                t.TextColor3 = Color3.new(1, 1, 1)
                                t.Font = Enum.Font.GothamBold
                                t.TextSize = 12
                                t.TextStrokeTransparency = 0
                                
                                task.spawn(function()
                                    while char:FindFirstChild("HumanoidRootPart") and self.States.playerEsp and p.Parent do
                                        if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                                            local dist = math.floor((LP.Character.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude)
                                            t.Text = p.Name .. " [" .. dist .. "m]"
                                        end
                                        task.wait(self.Config.playerEspUpdateSpeed)
                                    end
                                    if b and b.Parent then
                                        b:Destroy()
                                    end
                                end)
                                
                                self.Tracking.playerLabels[p] = b
                            end
                        end
                    end
                end)
                task.wait(self.Config.playerEspUpdateSpeed)
            end
        end)
        table.insert(self.Tracking.activeThreads, thread)
    end
end

-- ====== CLEANUP ======
function ApexScript:Cleanup()
    -- Desativar todos os states
    self.States.bringMetals = false
    self.States.bringItems = false
    self.States.speedHack = false
    self.States.fly = false
    self.States.itemEsp = false
    self.States.playerEsp = false
    self.States.multiplyFloors = false
    
    -- Parar threads ativas
    for _, thread in pairs(self.Tracking.activeThreads) do
        task.cancel(thread)
    end
    self.Tracking.activeThreads = {}
    
    -- Restaurar gravidade
    workspace.Gravity = self.OriginalGravity
    
    -- Restaurar velocidade
    if self:IsValidCharacter(LP.Character) then
        LP.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = 16
    end
    
    -- Limpar ESP
    for _, label in pairs(self.Tracking.espLabels) do
        if label and label.Parent then
            label:Destroy()
        end
    end
    
    for _, p in pairs(Players:GetPlayers()) do
        if p.Character and p.Character:FindFirstChild("PlayerHighlight") then
            p.Character.PlayerHighlight:Destroy()
        end
    end
    
    self.Tracking.espLabels = setmetatable({}, {__mode = "k"})
    self.Tracking.playerHighlights = setmetatable({}, {__mode = "k"})
    self.Tracking.playerLabels = setmetatable({}, {__mode = "k"})
end

-- ====== INICIALIZAÇÃO ======
function ApexScript:Initialize()
    print("[Sherlock Apex] Iniciando...")
    
    local contentFrame = self:BuildUI()
    
    -- Botões
    self:MakeButton("BRING METALS: OFF", function(btn) self:ToggleBringMetals(btn) end)
    self:MakeButton("BRING ITEMS: OFF", function(btn) self:ToggleBringItems(btn) end)
    self:MakeButton("SPEED HACK: OFF", function(btn) self:ToggleSpeedHack(btn) end)
    self:MakeButton("VOAR: OFF", function(btn) self:ToggleFly(btn) end)
    self:MakeButton("INF JUMP: OFF", function(btn) self:ToggleInfiniteJump(btn) end)
    self:MakeButton("MULTIPLY FLOORS: OFF", function(btn) self:ToggleMultiplyFloors(btn) end)
    
    self:MakeButton("INSTANT INTERACT", function(btn)
        if self:InstantInteract() then
            btn.Text = "INTERACT: SUCCESS"
            task.wait(2)
            btn.Text = "INSTANT INTERACT"
        else
            btn.Text = "INTERACT: NO EXPLOIT"
            task.wait(2)
            btn.Text = "INSTANT INTERACT"
        end
    end)
    
    self:MakeButton("ITEM ESP: OFF", function(btn) self:ToggleItemEsp(btn) end)
    self:MakeButton("PLAYER ESP: OFF", function(btn) self:TogglePlayerEsp(btn) end)
    self:MakeButton("EXIT", function() self:Cleanup(); self.UI.Hub:Destroy() end)
    
    -- Inicializar infinite jump listener
    self:InitializeInfiniteJump()
    
    print("[Sherlock Apex] Inicializado com sucesso!")
    print("[Sherlock Apex] Sistema: Detector Inteligente de Wooden Floor (padrão: digitos:digitos)")
end

-- ====== EXECUÇÃO ======
local apex = ApexScript.new()
apex:Initialize()
